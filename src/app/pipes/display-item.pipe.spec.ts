import { DisplayItemPipe } from './display-item.pipe';

describe('DisplayItemPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayItemPipe();
    expect(pipe).toBeTruthy();
  });
});
